name: Go CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout code
    - name: Checkout code
      uses: actions/checkout@v4

    # 2️⃣ Setup Go
    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version: "1.22"
        check-latest: true
        cache: true

    # 3️⃣ Install dependencies
    - name: Install dependencies
      run: go mod download

    # 4️⃣ Run tests
    - name: Run tests
      run: go test ./... -v || echo "⚠️ No tests found"

    # 5️⃣ Build Go binary
    - name: Build App
      run: go build -o ums-backend main.go

    # 6️⃣ Verify Build
    - name: Verify Build
      run: |
        if [ -f "ums-backend" ]; then
          echo "✅ Build successful!"
          ls -lh ums-backend
        else
          echo "❌ Build failed - binary not found"
          exit 1
        fi

    # 7️⃣ Deploy to Render
    - name: Deploy to Render
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      env:
        RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK }}
      run: |
        if [ -z "$RENDER_DEPLOY_HOOK" ]; then
          echo "⚠️ RENDER_DEPLOY_HOOK secret not configured"
          echo "✅ Render will auto-deploy via GitHub integration"
        else
          echo "🚀 Triggering manual deploy via webhook..."
          curl -X POST "$RENDER_DEPLOY_HOOK"
          echo "✅ Deploy request sent!"
        fi

    # 8️⃣ Deployment Info
    - name: Deployment Info
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ Build & Deployment pipeline completed!"
        echo "🔍 Check deployment status at: https://dashboard.render.com/"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
